import React from "react";

import styles from "./Loading.module.scss";

function Loading() {
  return (
    <section aria-label="Loading Content" className={styles.container}>
      Loading
    </section>
  );
}

export default Loading;
